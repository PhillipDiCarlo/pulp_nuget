# These files contain the well-known FAKE credentials that pulp's plugin_template
# ships for CI object-storage testing (minio's test key and Azurite's publicly
# documented development key). They are not real secrets.
paths-ignore:
  - ".github/workflows/scripts/before_install.sh"
  - ".gitleaks.toml"
  - "template_config.yml"
  - ".ci/**"
