Added the ``nuget.package`` content type. ``.nupkg`` files can be uploaded and all
metadata (id, version, authors, description, tags, license, dependency groups,
minClientVersion) is parsed from the embedded ``.nuspec``.
